#include<bits/stdc++.h>

using namespace std;
int ti[200001];
vector<int> soultion(int n, int a, int b,vector<vector<int>> request)
{
  vector<int> answer;
  set<int> s;
  for(auto cur :request)
  {
    if(s.size())


  }






  return answer;
}